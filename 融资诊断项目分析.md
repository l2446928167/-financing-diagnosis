# `l2446928167/-financing-diagnosis` 项目分析报告

> 分析对象：https://github.com/l2446928167/-financing-diagnosis
> 分析时间：2026-08-12 ｜ 仓库默认分支 `main` ｜ 主语言 Python ｜ 仓库大小约 20MB（主要是字体文件）

---

## 一、项目概览

这是一个面向**小微企业**的「AI + 融资诊断」Web 应用，本质是一个**智能信贷预评估 / 银企匹配工具**。

用户上传企业的财务报表（PDF / Excel / CSV），系统会：

1. 自动（或借助大模型）抽取关键财务指标；
2. 用一套**基于规则的 8 维健康评分引擎**对企业财务做体检；
3. 把企业画像与一个**银行信贷产品知识库**做规则匹配，给出"完全匹配 / 差距匹配"的产品清单；
4. 做**差距分析**，按"性价比"排出企业应优先补齐哪些条件才能解锁更多信贷产品；
5. 调用 DeepSeek 大模型生成自然语言诊断总结、风险点、改善建议与产品推荐；
6. 一键导出**中文 PDF 诊断报告**。

版本号当前为 `v1.5 (2026-08-12)`，作者持续在迭代（仓库刚创建于 2026-08-10）。

---

## 二、技术栈

| 类别 | 技术 / 库 | 用途 |
|------|-----------|------|
| Web 框架 | **Streamlit** | 整个应用的 UI 与入口（`app.py` 顶层顺序执行） |
| 数据处理 | **pandas** | 表格读写、产品库匹配、DataFrame 展示 |
| Excel 读写 | **openpyxl** | 解析上传的 `.xlsx/.xls` |
| PDF 解析 | **pdfplumber** | 从 PDF 抽取文本与表格（v1.5 由 PyPDF2 升级而来） |
| PDF 生成 | **reportlab** | 导出中文诊断报告（含中文 TTF 字体注册） |
| 大模型 | **openai** SDK（兼容 DeepSeek） | 调用 DeepSeek-V4-Flash 做指标抽取与诊断生成 |
| 配置 | **python-dotenv** | 本地保存/加载 API Key 到 `.env` |
| HTTP | **httpx** | 自定义 OpenAI 客户端超时 |
| 字体 | 仿真黑体/方正小标宋/楷体/仿宋 TTF | 解决 reportlab 中文乱码（仓库 20MB 几乎都是字体） |

> 依赖锁定在 `requirements.txt`：`streamlit, pandas, openpyxl, pdfplumber, reportlab, openai, python-dotenv, httpx`。

---

## 三、项目结构

```
-financing-diagnosis/
├── app.py                      # 入口：Streamlit 主程序（UI + 流程编排）
├── requirements.txt            # 依赖清单
├── README.md                   # 仅一行标题（文档缺失）
├── test_deepseek.py            # 独立脚本：测试 DeepSeek API 连通性
├── modules/                    # 核心业务模块
│   ├── __init__.py
│   ├── data_input.py           # 模块1：PDF/Excel/CSV 解析 + 指标抽取（正则 & AI）+ 会计校验
│   ├── diagnosis.py            # 模块2：8 维融资健康评分引擎（核心算法）
│   ├── product_matching.py     # 模块3：银行产品规则匹配
│   ├── gap_analysis.py         # 模块4：量化差距分析 + 行动优先级（性价比排序）
│   └── report_generator.py     # 模块5：PDF 报告生成
├── utils/
│   ├── llm_helper.py           # LLM 调用封装（call_llm / test_api_connection）
│   └── (data_extractor.py / vector_store.py 为空占位文件)
├── knowledge/
│   └── bank_products/
│       └── products.csv        # 银行信贷产品知识库（20 条产品，含准入条件）
├── data/sample/
│   └── test_finance.csv        # 示例财务数据
└── fonts/                      # 中文 TTF 字体（约 40MB 容量来源）
```

**空文件注意**：`utils/data_extractor.py`、`utils/vector_store.py` 是 0 字节占位文件，说明"向量检索 / RAG"路线被规划过但**尚未实现**——目前完全是基于规则的匹配，没有用到向量库。

---

## 四、核心功能（已实现）

1. **多格式财务数据录入**：支持 PDF（pdfplumber 抽文本+表格）、Excel、CSV；自动抽取或 AI 智能抽取 22 个财务指标。
2. **8 维金融健康评分**：规则引擎，输出 0–10 分 + 红黄绿灯 + 风险点 + 改善建议。
3. **会计恒等式校验**：自动校验"总资产≈总负债+所有者权益"和"流动比率≈流动资产÷流动负债"（容差 5%），并在 UI 展示偏差。
4. **AI 增强诊断**：DeepSeek 生成总体评价 / 风险点 / 改善建议，且 AI 失败时自动回退到规则引擎文本。
5. **银行产品匹配**：与 20 条产品知识库比对，输出"完全匹配 / 差距匹配"，法院执行记录为一票否决。
6. **差距分析与行动优先级**：量化企业每项指标与产品准入条件的差距，按"性价比 = 解锁产品数 ÷ 难度分"排序，给出优先行动清单与预计达标时间。
7. **AI 产品推荐说明**：基于匹配结果让大模型给出"最推荐哪 1–2 个产品 / 注意什么 / 优先补齐哪条条件"。
8. **中文 PDF 报告导出**：含企业概况、8 维评分、风险点、产品匹配表、AI 推荐、改善建议与免责声明。

---

## 五、融资诊断逻辑详解（最核心）

入口函数 `diagnose(metrics)`（`modules/diagnosis.py`），返回一个字典：
`overall_score / dimension_scores / traffic_lights / risks / suggestions / metrics_used`。

### 1) 8 个维度 + 权重（权重之和严格 = 1.00）

| 维度 | 权重 | 主要子指标 | 子权重 |
|------|------|-----------|--------|
| 现金流健康 | 0.18 | 经营现金流净额、现金流量比率、盈利现金比 | 0.3/0.4/0.3 |
| 偿债能力 | 0.14 | 资产负债率、流动比率、利息保障倍数(近似) | 0.4/0.3/0.3 |
| 盈利质量 | 0.12 | 毛利率、净利率、盈利现金比 | 0.35/0.35/0.3 |
| 运营效率 | 0.10 | DSO 应收账款周转天数、存货周转天数、总资产周转率 | 0.4/0.3/0.3 |
| 成长性 | 0.08 | 营收增长率、净利润增长率 | 0.5/0.5 |
| 经营稳定性 | 0.10 | 经营年限、营收规模、客户集中度 | 0.35/0.35/0.3 |
| 应收账款质量 | 0.08 | DSO、账龄结构（超 12 月占比） | 0.5/0.5 |
| 信用与融资 | 0.20 | 纳税评级、实控人征信、法院执行、融资机构数、融资利率、杠杆 | 见下 |

信用与融资维度的内部权重：纳税评级 0.20、实控人征信 0.20、法院执行 0.15、融资机构数 0.10、融资利率 0.15、杠杆 0.20。

### 2) 评分方法

- 每个子指标用**分段阈值映射**打分（如资产负债率 <30% → 10 分，30–50% → 8 分，… >85% → 2 分）。
- 缺值（`safe_float` 失败或为空）统一给**中间分 5 分**，避免惩罚未知项。
- 各子指标加权得到维度分（保留 1 位小数）。
- 总体分 = Σ(维度分 × 权重)，范围 0–10。

### 3) 红黄绿灯 & 风险/建议

- `traffic_light()`：≥7 绿、4–7 黄、<4 红。
- `generate_risks_and_suggestions()`：逐维度判断，分数 <4 给"严重"风险+建议，<7 给"偏弱"提示，最多各 8 条。
- UI 上对总体分也做了三档：≥7 整体健康、≥4 中等、<4 高风险。

> 这是一个**透明、可解释、纯规则**的评分卡，没有用到机器学习模型；AI 只负责"把分数翻译成人话"，不起评分作用。

---

## 六、产品匹配与差距分析

**`match_products(metrics, df_products)`**：遍历产品库逐条比对 9 类准入条件——营收门槛、成立年限、纳税评级（A>B>M>C>D 排序）、融资机构数上限、资产负债率上限、流动比率下限、抵押要求、行业限制、法院执行（一票否决）。

- 无差距 → 🟢 完全匹配；差距 ≤2 条或被否决 → 🟡 差距匹配；差距 >2 条 → 直接跳过不展示。
- 产品库 `products.csv` 共 20 条（建行/工行/农行/招行/平安/中行/中信/邮储/浦发/北京银行/国开行/交行/兴业/光大/浙商 等），含额度区间、利率区间、期限、各项准入门槛与数据来源/采集日期。

**`analyze_gaps()`**：把每个产品的差距拆成结构化条目（当前值/准入要求/差距量/难度分/行动），按"行动项"聚合，计算**性价比 = 解锁产品数 ÷ 难度分**（"不可控"如经营年限难度分 99），按性价比降序排出行动优先级，并估算达标时间（如"规范纳税 1–2 年""提供材料即可"）。

---

## 七、数据录入与 AI 抽取（`modules/data_input.py`）

- `parse_financial_data()` 按扩展名分流到 `extract_from_csv / extract_from_excel / extract_from_pdf`。
- `auto_extract_metrics()`：基于 `FIELD_ALIASES`（别名映射，如"资产总计→总资产"）的正则 `关键词.*?([0-9,]+\.?\d*)` 快速抽取；DataFrame 优先按列名匹配。
- `llm_extract_metrics()`：调用 DeepSeek 抽取 22 个字段（新 Schema：`{value, unit, page}`），支持：
  - 表格优先；短文本（≤2 万字符）直接抽；长文本分块（18000 字符/块，500 重叠）→ 逐块摘要 → 浓缩 → 最终抽取。
  - **单位归一化** `normalize_units()`（元/亿元统一换算到万元）。
  - **会计校验** `verify_accounting()`（恒等式 + 流动比率校验，容差 5%）。
  - **二次补提** `_supplement_missing_metrics()` 对空字段正则定向补搜；有流动资产/负债时自动计算流动比率。

---

## 八、架构与入口

- **入口**：`app.py` 即 Streamlit 应用，无独立 `main()`，顶层顺序执行；通过 `streamlit run app.py` 启动。
- **架构模式**：典型的**单体脚本 + 模块拆分**，前端(Streamlit) 与业务逻辑(diagnosis/product_matching/gap_analysis/report_generator) 通过 `import` 直接调用，所有中间状态放在 `st.session_state`。
- **调用链**：
  ```
  app.py
   ├─ modules.data_input.parse_financial_data / auto_extract_metrics / llm_extract_metrics
   ├─ modules.diagnosis.diagnose            → 8 维评分 + 风险/建议
   ├─ utils.llm_helper.call_llm             → DeepSeek 诊断文本/推荐
   ├─ modules.product_matching.match_products
   ├─ modules.gap_analysis.analyze_gaps
   └─ modules.report_generator.generate_pdf
  ```
- **LLM 接入**：`utils/llm_helper.py` 用 OpenAI SDK 指向 `https://api.deepseek.com`，模型 `deepseek-v4-flash`，支持 thinking 模式下 content 为空时回退读 `reasoning_content`。

---

## 九、配置与部署

- **配置**：
  - API Key 通过侧边栏输入，用 `python-dotenv` 的 `set_key` 写入项目根目录 `.env`（`DEEPSEEK_API_KEY`），下次启动自动加载；并带"测试 API 连接"按钮。
  - 模型目前**写死**为 `DeepSeek-V4-Flash`（下拉框只有一个选项）。
- **部署**：
  - 无 Dockerfile / CI / Procfile，本地 `pip install -r requirements.txt && streamlit run app.py` 即可。
  - 字体目录 `fonts/` 必须随项目部署（reportlab 注册中文 TTF，缺失则中文乱码、回退 Helvetica）；作者注释提到若部署到 Streamlit Cloud 需确保 `fonts/` 上传。
  - 没有后端服务 / 数据库 / 用户系统，全部会话状态在内存（`st.session_state`）中，单用户本地使用。

---

## 十、总体评价

**亮点**
- 业务逻辑完整、可解释性强：8 维评分卡 + 会计校验 + 差距分析"性价比排序"是相当实用的信贷预评估思路。
- 工程细节到位：单位归一化、长文本分块抽取、AI 失败回退规则引擎、中文 PDF 字体处理。
- 产品库带数据来源与采集日期，可追溯。

**不足 / 可改进点**
1. **README 几乎为空**，无使用说明、无架构图、无示例截图。
2. 模型**硬编码** DeepSeek，且 `test_deepseek.py` 用的是 `reasoning_effort="high"` + thinking 的写法，与实际 `llm_helper` 略有出入。
3. `utils/data_extractor.py` / `vector_store.py` 为空，规划中的**向量检索 / RAG** 未落地，产品匹配纯规则。
4. 评分阈值（如资产负债率 0.3/0.5/0.7/0.85 分档）为**经验硬编码**，缺乏校准与样本验证；缺值一律 5 分可能高估"未知"企业。
5. 产品库仅 20 条且为静态 CSV，需手工更新"采集日期"。
6. 无测试、无类型注解、无错误处理统一规范；`diagnosis.py` 等函数无 docstring 之外的一致性测试。

**结论**：这是一个定位清晰、完成度高、偏"规则引擎 + LLM 润色"的小微企业融资诊断原型，适合作为信贷获客/预评估工具的 MVP；若投入生产，建议补充文档、抽象模型配置、引入真实样本校准阈值，并把知识库改为可在线更新的数据源。
