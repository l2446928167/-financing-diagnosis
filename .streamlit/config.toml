# Streamlit 主题配置（v3.0 对话式重做）
# 设计令牌：现代 fintech 蓝主色 + 柔和灰底 + 白卡片；语义红黄绿仅用于诊断徽章（在 ui_style.py 中定义）。
[theme]
primaryColor = "#2F54EB"
backgroundColor = "#F5F6F8"
secondaryBackgroundColor = "#FFFFFF"
textColor = "#1F2733"
font = "sans serif"
