# 本文件仅作模板，复制为 .env 后填入真实密钥（.env 已被 .gitignore 忽略，不会进 git）。
# 部署到 Streamlit Cloud / Docker 时，无需此文件，直接在 Secrets / 环境变量里配置即可。
#
# 统一 API Key（服务端加载，对所有用户透明，无需任何用户在界面填写）：
DEEPSEEK_API_KEY=sk-your-deepseek-key-here
